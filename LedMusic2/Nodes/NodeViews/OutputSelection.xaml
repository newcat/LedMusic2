<UserControl x:Class="LedMusic2.Nodes.NodeViews.OutputSelection"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:LedMusic2.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="45" d:DesignWidth="300">
    <StackPanel>
        <TextBlock Text="Output: " Height="20" />
        <ComboBox ItemsSource="{Binding Outputs, Source={x:Static vm:MainViewModel.Instance}}" SelectedItem="{Binding SelectedOutput, Mode=TwoWay}">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>
    </StackPanel>
</UserControl>
